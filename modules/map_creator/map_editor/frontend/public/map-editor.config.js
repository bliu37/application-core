window.MAP_EDITOR_CONFIG = {
  // backendHost can be set after build, for example:
  // backendHost: "127.0.0.1:58000"
  // backendPort is used by development defaults when backendHost is empty.
  backendHost: "",
  backendPort: "58000"
};
